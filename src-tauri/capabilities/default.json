{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs and opening links in the system browser.",
  "windows": ["main"],
  "permissions": ["core:default", "opener:default"]
}
